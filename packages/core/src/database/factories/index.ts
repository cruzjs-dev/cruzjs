/**
 * Database Factories
 *
 * Re-exports the core `defineFactory` helper and its types.
 * Consumer packages and application code can import from here:
 *
 *   import { defineFactory } from '@cruzjs/core/database/factories';
 */

export { defineFactory } from './factory';
export type { Factory } from './factory';
